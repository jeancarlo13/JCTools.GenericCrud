@using System.Collections.Generic;
@using System.Collections;
@using System.Linq;
@model Test3._1.Models.Movie

@{
    var country = (ViewBag.Countries as IEnumerable<Test3._1.Models.Country>)
            .FirstOrDefault(c => c.Id == Model.Country);
}
@(country?.Name ?? string.Empty)