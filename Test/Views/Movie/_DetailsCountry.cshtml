@using System.Collections.Generic;
@using System.Collections;
@using System.Linq;
@model Test.Models.Movie

@{
    var country = (ViewBag.Countries as IEnumerable<Test.Models.Country>)
            .FirstOrDefault(c => c.Id == Model.Country);
}
@(country?.Name ?? string.Empty)