@using System.Collections.Generic;
@using System.Collections;
@using System.Linq;
@model Test5._0.Models.Movie

@{
    var country = (ViewBag.Countries as IEnumerable<Test5._0.Models.Country>)
            .FirstOrDefault(c => c.Id == Model.Country);
}
@(country?.Name ?? string.Empty)